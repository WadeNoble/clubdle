import { useState } from "react";
import gameData from "../src/data/data.json"

/*const useLogic = (solution) => {
  const [turn, setTurn] = useState(0);
  const [currentGuess, setCurrentGuess] = useState("");
  const [guesses, setGuesses] = useState([]); // each guess is an array
  const [history, setHistory] = useState([]); // each guess is a string
  const [isCorrect, setIsCorrect] = useState(false);

  /* convert name guess into array of game information
    ["year":'2009', color:'yellow']
    */
 // const formatGuess = () => {
    
// };
  /* add new guess to list of guesses
   set to Correct if correct
   add one to turn state*/
 // const addNewGuess = () => {

 // };
  /* track current guess
   if user submits, call addnewguess */
//  const checkValue = () => {

 /* };
  return {turn, currentGuess, guesses, isCorrect, checkValue}
};

export default useLogic;*/
